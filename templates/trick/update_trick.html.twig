{% extends 'base.html.twig' %}


{% block title %}Éditer une figure{% endblock %}

{% block body %}

    <div class="imgCarouss">
        <div class="imgprinc">
            {% for img in trick.medias %} 
                {% if img.type == 'imgP' %}
                    <img src="{{ asset(img.mediaPath) }}">
                {% endif %}
            {% endfor %}
        </div>

        <div class="imgSec">
          {% for media in trick.medias %} 
              <a class="page-link" href="{{ path('display_media', {id:media.id}) }}" target="_blank"><img src="{{ asset(media.mediaPath) }}" style = "max-width: 150px;"></a>
          {% endfor %}
        </div>
    </div>

    <hr style="color: #18bc9c; border:solid 0.5px;">

<div class="col-md-12">
    <div class="box box-primary">
        <div class="box-header">
            <h4 class="box-title">Mettre à jour</h4>
        </div>

        {{include('trick/_trick_form.html.twig')}}

    </div>

<form method="post" action="{{ path('delete_trick', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <button class="btn" style="background-color:#e74c3c; color:white;">Supprimer le trick</button>
</form>
{% endblock %}