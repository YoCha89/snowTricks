{% extends 'base.html.twig' %}


{% block title %}Supprimer la figure{% endblock %}

{% block body %}

<div id="delCheck">
    <p>Vous êtes sur le point de supprimer le trick : <span style="font-weight: bold;">{{trick.name}}</span>.<br/> Confirmez-vous la suppression ? </p>
</div>

<div id="delConf">
    <form method="post" action="{{ path('delete_trick', {'slug': trick.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="checkDone" value="checkDone">
    <button class="btn" style="background-color:#e74c3c; color:white;">Supprimer le trick</button>
    </form>
</div>

{% endblock %}