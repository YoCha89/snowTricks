
<div class="box-body">
    {{ form_start(form) }}
    <div class="part mb-2" style="display: flex; justify-content: space-around; margin-bottom: 2em;">
        <section class="model" style="width: 50%;">
            {{ form_row(form.name) }}
        </section>

        <section class="capacity" style="width: 45%;">
            {{ form_row(form.content) }}
        </section>

        <section class="capacity" style="width: 45%;">
            {{ form_row(form.media.title) }}
        </section>
    </div>

    <div class="button">
        <button type="submit" class="btn btn-primary"> <i class="fa fa-save"></i>Ajouter la figure</button>
    </div>

    {{ form_end(form) }}
</div>