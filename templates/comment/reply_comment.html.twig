{% extends 'base.html.twig' %}


{% block body %}

  <div class="card text-white bg-dark mb-3" style="max-width: 20rem;">
    <div class="card-body">
      <div class="d-flex w-100 justify-content-between">
        <h5 class="mb-1">{{ commentParent.account.fullName }}</h5>
        <small class="text-muted">{{ commentParent.createdAt|date("d/m/Y") }}</small>
      </div>
      <p class="card-text"> {{ commentParent.content }} </p>
    </div>

    <h3>Votre réponse : </h3>
    <div class="form-group">
      {% include 'comment/_comment_form.html.twig' with {'form': form} %}
    </div>   
  </div>

{% endblock %}